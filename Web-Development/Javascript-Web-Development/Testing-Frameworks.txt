Jest.js - Testing UI Framework
	// Test File Format:
		// *.test.js
		// *.spec.js
	// Snapshot Testing - Renders the DOM, then takes a snapshot of the App Component DOM tree, and saves it to the local file.
		// Need to commit with Git
		// Used as a check in subsequent testing, to see if current snapshot matches with saved snapshot.
			// Fail if 2 images do not match
			// Fail if change is unexpected
			// Fail if screenshot needs to be updated to the new version of the UI Component
	// Mocking - simulates a function, object, model, imports
		// jest.fn()
		// jest.mock()
		
Enzyme.js - A Testing UI Framework that simulates input Events to test Presentational Components
	// shallow(<React Component>).simulate(<event>, <eventArgs>)
		// Shallow Rendering - Only component itself without its children, but with parents included
	// enzyme-to-json to convert Enzyme wrappers for Jest snapshot matcher